{
  "name": "risk-management-dashboard",
  "version": "1.0.0",
  "description": "Broker Risk Management Dashboard",
  "main": "index.js",
  "scripts": {
    "start": "live-server --port=3000 --open=index.html",
    "dev": "npx live-server --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
