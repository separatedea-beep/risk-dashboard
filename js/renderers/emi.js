// EMI balances are currently static HTML.
// This renderer is a placeholder for when you connect real EMI balance APIs.

export function renderEmi() {
  // Future: update EMI balance values from API
  // setVal('emi-mu', fmt(data.mauritius_balance));
  // setVal('emi-sg', fmt(data.singapore_balance));
  // setVal('emi-hk', fmt(data.hongkong_balance));
  // setVal('emi-total', fmt(data.total_pool));
  // setVal('emi-surplus', fmt(data.surplus));
}
